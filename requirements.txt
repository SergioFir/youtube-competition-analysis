# Supabase client
supabase>=2.0.0

# HTTP requests (for YouTube API and Shorts detection)
httpx>=0.27.0

# Environment variables
python-dotenv>=1.0.0

# XML parsing (for RSS feeds)
feedparser>=6.0.0

# Scheduling
schedule>=1.2.0

# Logging (optional, stdlib is fine but this is nicer)
loguru>=0.7.0

# Web framework (for WebSub webhooks)
fastapi>=0.115.0
uvicorn>=0.32.0

# Excel file reading (for seed data import)
openpyxl>=3.1.0

# Trend detection
youtube-transcript-api>=0.6.0  # Fetch video transcripts
openai>=1.0.0                  # OpenRouter uses OpenAI-compatible API
